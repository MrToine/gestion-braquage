import { MilFormatPipe } from './mil-format.pipe';

describe('MilFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MilFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
